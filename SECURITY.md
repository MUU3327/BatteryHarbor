# 安全策略

电池港包含以 root 身份运行的 Helper，并能够写入 AppleSMC。任何权限校验、XPC 身份认证、SMC 白名单、回读验证或失败恢复问题，都应按安全问题处理。

## 支持范围

项目尚处于 `0.1.x` 技术预览阶段。安全修复只保证应用到最新源码；尚未发布的旧构建不提供长期支持。

## 报告安全问题

公开仓库创建后，请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue 中披露可绕过 root Helper 校验、执行任意 SMC 写入或伪造 XPC 客户端身份的方法。

报告中建议包含：

- 受影响的版本或提交
- macOS 版本、Mac 型号和芯片
- 可复现的最小步骤
- 预期安全边界与实际行为
- 已脱敏的日志或诊断信息

请勿提交 Apple ID、设备序列号、用户文件内容、完整用户名路径或签名证书私钥。

## 安全边界

- 主 App 不直接写入 SMC。
- Helper 必须以 root 身份运行，并验证客户端 Bundle ID 与代码签名。Apple 签名构建要求 App 与 Helper 的 Team ID 一致；自签名社区构建要求两者的叶证书字节完全一致；ad-hoc 构建不得连接 Helper。
- 仅允许固定白名单中的已知 SMC 键和值。
- Helper 每次启动后默认锁定，恢复自检通过前拒绝控制命令。
- 写入必须回读；任一步失败都应恢复原值并保持锁定。
- 自动化测试和 GitHub Actions 不得触发硬件写入。
