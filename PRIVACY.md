# 隐私说明

本说明适用于电池港 `0.1.0` 当前源码。

## 网络与遥测

当前版本不包含网络请求、账户系统、广告、遥测或第三方分析 SDK。电池与能耗数据不会由电池港自动上传。

## 本机读取的数据

为了显示状态和执行控制，App 会读取：

- 电量、充放电状态、温度、容量、健康度、循环次数、电压和电流
- 适配器输入、系统负载和电池功率
- Mac 型号、系统版本、AppleSMC 服务与相关能力
- 本机运行中 App 的名称、Bundle 路径、CPU、唤醒和 I/O 指标，用于能耗排行

App 不读取用户文档内容，也不读取或保存 Apple ID 与电池序列号。

## 本机保存的数据

- 充电上限、界面偏好、保护选项、计划任务、执行日志和校准状态保存在 App 的 UserDefaults 中。
- 最近 24 小时的功率与 App 能耗样本保存在 `~/Library/Application Support/BatteryHarbor/EnergyHistory.json`。
- 历史数据可在设置中清除。

App 能耗历史可能包含应用名称和 Bundle 路径。若应用位于用户自定义目录，路径可能间接包含本机用户名；分享 CSV 前请自行检查并脱敏。

## 用户主动导出

只有用户点击导出时，App 才会通过系统保存面板写出：

- 能耗历史 CSV
- 诊断报告文本

诊断报告的设计目标是不包含 Apple ID、电池序列号或用户文件内容。分享前仍建议检查系统、机型、固件、Bundle ID、Helper PID 和本地路径等信息是否符合你的隐私要求。

## 系统权限

- 登录项权限用于可选的登录时启动。
- root Helper 仅用于受限的电池充电控制；它不提供通用命令执行接口。

未来若加入网络功能或数据收集，本文件必须在功能发布前更新。
